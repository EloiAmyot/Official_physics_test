using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class RayCast : MonoBehaviour
{
    //private float rotationSpeed = 20f;
    //private Vector3 horizontalMouvement;
    //Start is called before the first frame update
    void Start()
    {
        
    }
        
    // Update is called once per frame
    void Update()
    {
        //horizontalMouvement = new Vector3(0f, 0f, -Input.GetAxis("Horizontal"));
        //transform.Rotate(horizontalMouvement * rotationSpeed * Time.deltaTime);
        //if (Input.GetKey(KeyCode.Space))
        //{
        //    RaycastHit2D hit = Physics2D.Raycast(transform.position, transform.TransformDirection(Vector2.up), 10f);
        //   
        //    Debug.DrawRay(transform.position, transform.TransformDirection(new Vector2(0f, 10f)));
        //    if (hit)
        //    {
        //        Debug.Log("hit");
        //    }
        //}
        
    }
}
